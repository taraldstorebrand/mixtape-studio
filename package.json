{
  "name": "mixtape-studio",
  "version": "1.0.0",
  "private": true,
  "description": "Mixtape Studio - Build your own mixtapes",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev -C backend\" \"npm run dev -C frontend\"",
    "build": "npm run build -C backend && npm run build -C frontend",
    "install:all": "npm install -C backend && npm install -C frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
